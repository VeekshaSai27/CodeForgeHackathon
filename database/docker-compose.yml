# ⚠️  This file is retired.
#
# PostgreSQL is now managed by the root docker-compose.yml alongside
# the api and frontend containers.
#
# To start everything:
#   docker compose up --build          (from project root)
#
# To start only the database locally:
#   docker compose up postgres         (from project root)
